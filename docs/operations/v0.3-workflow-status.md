# KnowNest V0.3 工作流状态

## 当前模式

- 主控窗口生成任务提示词，后续可切自动调度。
- 当前文件用于记录 V0.3 开发过程、阶段进度、验证结果、审核结论和下一步任务。
- V0.3 agent 执行前应先读取 `docs/operations/v0.3-agent-context.md`，再按本文件确认当前阶段。
- 每完成一个 Task，需要在本文件补充完成情况、验证命令、提交 hash、剩余风险和下一步建议。

## 当前阶段进度

- V0.3 准备阶段：已完成。
- V0.3 文档结构整理：已完成并提交，commit `3fe5426`。
- V0.3 专用 agent context：已创建，路径 `docs/operations/v0.3-agent-context.md`。
- V0.3 专用 workflow status：当前文件，已创建。
- 当前待执行任务：Task 7 编辑页 AI 助手接入。
- 当前结论：Task 7A 已完成并提交；可以继续原 Task 7 编辑页 AI 助手接入。

## 已完成阶段和任务

- 准备阶段：
  - 已阅读 V0.3 PRD。
  - 已结合现有 Next.js / Supabase / Drizzle 代码结构生成技术设计文档。
  - 已生成 V0.3 实施计划文档。
  - 已整理 `docs/` 目录结构。
  - 已将 V0.3 上下文摘要独立为 `docs/operations/v0.3-agent-context.md`。
- Task 1：数据库 migration 与类型同步：
  - 已创建 `db/migrations/0003_ai_assistant.sql`。
  - 用户已确认在目标数据库手动执行该 SQL。
  - 已为 `knowledge_items` 增加 `summary`、`summary_generated_at`、`ai_updated_at` schema 字段。
  - 已新增 `ai_usage_logs` Drizzle schema，包含 action/status check、用户外键和知识归属复合外键。
  - 已同步 `KnowledgeItem` 类型。
  - 已允许 `updateKnowledgeItem()` 更新 AI 摘要相关字段。
  - 已更新 `src/lib/db/knowledge-items.test.mjs` 覆盖新增 schema 字段和 update payload。
  - 验证命令：`npm run test:knowledge-items`。
  - 验证结果：通过。
  - 实现提交 hash：`9ffc47b`。
  - 剩余风险：`ai_usage_logs` repository、限流和 API 闭环将在后续 Task 实现。
- Task 2：AI 类型、配置、错误和 Schema：
  - 已创建 `src/types/ai.ts`，包含 AI action、请求、结果和错误码类型。
  - 已创建 `src/lib/ai/config.ts`，支持 DeepSeek 配置读取、默认值和 action 到模型的映射。
  - 已创建 `src/lib/ai/errors.ts`，包含 `AIRequestError` 和统一错误响应映射。
  - 已创建 `src/lib/ai/schemas.ts`，支持 AI JSON 解析和五类 action 返回结构校验。
  - 已创建 `src/lib/ai/config.test.mjs`、`src/lib/ai/errors.test.mjs`、`src/lib/ai/schemas.test.mjs`。
  - 已在 `package.json` 增加 `test:ai` 脚本。
  - 验证命令：`npm run test:ai`。
  - 验证结果：通过。
  - 额外验证命令：`npm run build`。
  - 额外验证结果：通过。
  - 实现提交 hash：`8d96b82`。
  - 剩余风险：真实模型名仍需在实际接入前按官方可用模型确认；Prompt 与 DeepSeek Provider 已在 Task 3 处理。
- Task 3：Prompt 与 DeepSeek Provider：
  - 已安装 `openai` SDK，并更新 `package.json`、`package-lock.json`。
  - 已创建 `src/lib/ai/provider.ts`，定义 AI Provider 通用接口。
  - 已创建 `src/lib/ai/prompts.ts`，为五类 AI action 构造 system/user messages。
  - 已创建 `src/lib/ai/deepseek.ts`，封装 OpenAI-compatible DeepSeek client，使用 JSON response format 并解析返回 JSON。
  - 已创建 `src/lib/ai/prompts.test.mjs`，覆盖 JSON 要求、已有标签、已有分类和正文整理 Prompt。
  - 已更新 `test:ai` 脚本纳入 Prompt 测试。
  - 验证命令：`npm run test:ai`。
  - 验证结果：通过。
  - 构建验证命令：`npm run build`。
  - 构建验证结果：通过。
  - 实现提交 hash：`ced98ab`。
  - 剩余风险：尚未实现 AI 使用日志 repository、限流和 `/api/ai` 调用闭环；真实 DeepSeek 调用需要后续配置 API Key 后验证。
- Task 4：AI 使用日志与限流：
  - 已创建 `src/lib/db/ai-usage-logs.ts`，实现 AI 使用日志写入、UTC 当日计数、日志 payload 归一化。
  - 已创建 `src/lib/db/ai-usage-logs.test.mjs`，覆盖 `aiUsageLogs` schema、用户外键、知识归属复合外键、日志 payload 和 UTC 当日范围。
  - 已创建 `src/lib/ai/usage-limits.ts`，实现每日次数限制和输入正文长度校验。
  - 已创建 `src/lib/ai/usage-limits.test.mjs`，覆盖允许、超限、空正文、过短、过长和正常输入。
  - 已更新 `test:ai` 脚本纳入 usage limits 和 ai usage logs 测试。
  - 验证命令：`npm run test:ai`。
  - 验证结果：通过。
  - 额外验证命令：`npm run build`。
  - 额外验证结果：通过。
  - 实现提交 hash：`3e2c5e1`。
  - 剩余风险：`/api/ai` 尚未接入日志、限流、Provider 和权限校验；失败日志写入闭环将在 Task 5 实现。
- Task 5：统一 `/api/ai` 路由：
  - 已创建 `src/lib/ai/actions.ts`，实现 AI 请求解析和上下文组装。
  - 已创建 `src/lib/ai/actions.test.mjs`，覆盖无效 action、缺少内容和 ID、保留 `knowledgeItemId`、编辑器内容优先、保存内容回退和无权限场景。
  - 已创建 `src/app/api/ai/route.ts`，串起 `requireUser()`、配置读取、请求解析、知识归属校验、输入长度校验、每日限流、DeepSeek Provider、结果校验和成功/失败日志。
  - 已更新 `test:ai` 脚本纳入 actions 测试。
  - 验证命令：`npm run test:ai`。
  - 验证结果：通过。
  - 构建验证命令：`npm run build`。
  - 构建验证结果：通过。
  - 实现提交 hash：`46be12b`。
  - 剩余风险：真实 DeepSeek API Key 和 Vercel 环境变量尚未配置；真实模型调用、失败日志和限流需要后续手动验收验证。
- Task 6：AI 摘要应用与展示：
  - 已新增详情页 AI 摘要生成、预览、取消和应用基础流程。
  - 已新增 `AIAssistantPanel` / `AIResultPreview`，当前仅支持 `generate_summary`。
  - 已新增摘要应用 server action，应用时通过当前用户更新 `summary`、`summary_generated_at`、`ai_updated_at`。
  - 已调整列表 view model 优先展示非空 AI 摘要；空白或无摘要时回退正文截断。
  - 已在详情页正文前展示 AI 摘要和生成时间；无摘要时展示“暂无 AI 摘要”。
  - 已修复重新生成失败后旧摘要预览仍可应用的问题。
  - 已补充摘要列表、AI assistant model、summary payload/revalidation/action executor 测试。
  - 验证命令：`node --experimental-strip-types --disable-warning=MODULE_TYPELESS_PACKAGE_JSON src\components\ai\ai-assistant-panel-model.test.mjs`。
  - 验证结果：通过。
  - 额外验证命令：`node --experimental-strip-types --disable-warning=MODULE_TYPELESS_PACKAGE_JSON src\lib\knowledge\knowledge-item-summary.test.mjs`。
  - 额外验证结果：通过。
  - 额外验证命令：`npm.cmd run test:knowledge-list-item`。
  - 额外验证结果：通过。
  - 构建验证命令：`npm.cmd run build`。
  - 构建验证结果：通过；本地无 Supabase public env 时 build 会失败，最终使用非真实占位值 `NEXT_PUBLIC_SUPABASE_URL='https://example.supabase.co'` 与 `NEXT_PUBLIC_SUPABASE_ANON_KEY='placeholder-anon-key'` 运行通过。
  - Diff 验证命令：`git diff --check 9511333..HEAD`。
  - Diff 验证结果：通过。
  - 实现提交 hash：`e8be66f120f74f1500935f878ee965aa98597b28`。
  - Review 结果：Spec review 最终通过，未发现缺失或越界；Quality review 最终通过，无 Critical/Important/Minor。
  - 剩余风险：未做真实 DeepSeek 端到端调用；真实模型调用仍依赖本地/线上 AI env。Task 6 只完成详情页摘要闭环；编辑页/新建页 AI 助手、标签/分类、标题/正文整理仍在后续 Task。
- Task 7A：OpenAI-compatible Provider Registry 与设置页模型选择：
  - 已新增服务端 allowlist registry，用于集中声明可用 AI 模型和 Provider。
  - 已新增 OpenAI-compatible provider factory，支持按 registry 配置创建兼容 OpenAI SDK 的 Provider。
  - 已更新 `/api/ai`，对请求中的 `modelId` 执行 allowlist 校验并按选中模型路由 Provider。
  - 已在设置页新增本地 `localStorage` 模型选择器，保存用户当前 AI 模型偏好。
  - 已更新 AI assistant 调用链，发送当前选中的 `modelId` 到 `/api/ai`。
  - 已更新 `.env.example`，补充 Xiaomi MiMo Token Plan 的非真实配置示例。
  - 验证命令：`npm run test:ai`。
  - 验证结果：通过。
  - 额外验证命令：`node --experimental-strip-types --disable-warning=MODULE_TYPELESS_PACKAGE_JSON src/app/app/settings/settings-model.test.mjs`。
  - 额外验证结果：通过。
  - 额外验证命令：`npm run test:knowledge-list-item`。
  - 额外验证结果：通过。
  - 构建验证命令：`npm run build`。
  - 构建验证结果：通过。
  - Diff 验证命令：`git diff --check`。
  - Diff 验证结果：通过。
  - 实现提交 hash：`76c16fa996e0edee6f9af3e3d6bbae38e3613db3`。
  - 实现范围：`80da109..76c16fa`；Task 7A 起点到最新实现为 `ec85b7a..76c16fa`。
  - Review 结果：整体 spec review 通过；整体 quality review 通过。
  - 剩余风险：未做真实 Xiaomi MiMo Token Plan 端到端调用；真实 endpoint/key 需要部署者在本地/线上手动配置并验收；settings panel 交互无组件级测试但核心选择 helper 已覆盖。
  - 下一步建议：继续原 Task 7 编辑页 AI 助手接入。

## 当前参考文档

| 类型 | 路径 | 用途 |
|---|---|---|
| Agent 上下文 | `docs/operations/v0.3-agent-context.md` | V0.3 执行者快速了解目标、约束和设计边界 |
| PRD | `docs/versions/v0.3/prd.md` | 产品需求和验收标准 |
| 技术设计 | `docs/versions/v0.3/technical-design.md` | 架构、接口、数据库、安全和测试策略 |
| 实施计划 | `docs/versions/v0.3/implementation-plan.md` | Task 1 到 Task 11 的开发步骤 |
| 总工作流状态 | `docs/operations/workflow-status.md` | V0.1/V0.2 历史状态 |

## 最新关键技术决策

- V0.3 首发接入 DeepSeek API，并通过服务端 allowlist registry 支持 OpenAI-compatible Provider 扩展。
- AI 调用统一走服务端 `POST /api/ai`。
- 前端不得暴露 AI Provider API Key，不允许使用 `NEXT_PUBLIC_DEEPSEEK_API_KEY` 或其他 public key 变量承载模型密钥。
- `/api/ai` 只生成候选结果，不直接覆盖知识数据。
- `/api/ai` 必须校验 `modelId` allowlist，禁止客户端传入任意模型或 endpoint。
- 用户必须预览并确认后，AI 结果才能应用到摘要、标签、分类、标题或正文。
- V0.3 需要新增 AI 使用日志和基础每日限流。
- 数据隔离继续坚持 `requireUser()` + `src/lib/db` repository 显式 `userId` 过滤，Supabase RLS 作为兜底。

## 当前约束

- 不要直接开始实现 Task 8 及后续任务，除非 Task 7 已完成并记录验证结果。
- 不要改线上配置，不要改 Vercel 环境变量。
- 不要把真实 DeepSeek API Key 写入源码、文档或提交历史。
- 业务代码中不得新增前端直连 DeepSeek 的逻辑。
- 页面和业务组件不得直接调用 Supabase SDK、SQL client 或 ORM。
- 数据库访问走 `src/lib/db`，认证访问走 `src/lib/auth`。
- 每个阶段完成后必须运行最小相关验证；无法验证时记录原因。

## 下一个建议任务

### Task 7：编辑页 AI 助手接入

执行范围：

- 修改 `src/app/app/items/[id]/knowledge-item-editor.tsx`。
- 修改 `src/app/app/items/new/knowledge-form.tsx`。
- 修改 `src/components/ai/ai-assistant-panel.tsx`。
- 修改 `src/components/ai/ai-result-preview.tsx`。

实施要求：

- 编辑页标题改为受控状态。
- 编辑页分类改为受控状态。
- 编辑页插入 `AIAssistantPanel`，传入当前标题、正文、分类、标签和应用回调。
- 新建页接入未保存内容 AI，不传 `itemId`。
- 摘要在编辑页和新建页先只预览，不自动写入隐藏字段。

验证要求：

```bash
npm run build
```

完成后更新本文件，记录 Task 7 状态、变更摘要、验证命令和结果、提交 hash、剩余风险与下一步建议。

## 阶段审核记录

| 节点 | 状态 | 备注 |
|---|---|---|
| V0.3 准备阶段 | 已完成 | 已生成 PRD、技术设计、实施计划，并整理 docs 目录结构。 |
| V0.3 workflow 初始化 | 已完成 | 已拆分 agent context 与 workflow status 职责。 |
| Task 1 数据库 migration 与类型同步 | 已完成 | 实现提交 `9ffc47b`；验证 `npm run test:knowledge-items` 通过；用户已确认 SQL 手动执行完成。 |
| Task 2 AI 类型、配置、错误和 Schema | 已完成 | 实现提交 `8d96b82`；验证 `npm run test:ai` 和 `npm run build` 通过。 |
| Task 3 Prompt 与 DeepSeek Provider | 已完成 | 实现提交 `ced98ab`；验证 `npm run test:ai` 和 `npm run build` 通过。 |
| Task 4 AI 使用日志与限流 | 已完成 | 实现提交 `3e2c5e1`；验证 `npm run test:ai` 和 `npm run build` 通过。 |
| Task 5 统一 `/api/ai` 路由 | 已完成 | 实现提交 `46be12b`；验证 `npm run test:ai` 和 `npm run build` 通过。 |
| Task 6 AI 摘要应用与展示 | 已完成 | 实现提交 `e8be66f120f74f1500935f878ee965aa98597b28`；验证摘要相关测试、`npm.cmd run test:knowledge-list-item`、`npm.cmd run build` 和 `git diff --check 9511333..HEAD` 通过。 |
| Task 7A OpenAI-compatible Provider Registry 与设置页模型选择 | 已完成 | 实现提交 `76c16fa996e0edee6f9af3e3d6bbae38e3613db3`；范围 `80da109..76c16fa` / `ec85b7a..76c16fa`；验证 `npm run test:ai`、settings model 单测、`npm run test:knowledge-list-item`、`npm run build` 和 `git diff --check` 通过；整体 spec review 与 quality review 通过。 |
| Task 7 编辑页 AI 助手接入 | 待开始 | 下一步接入编辑页和新建页 AI 助手。 |

## 验证记录

| 日期 | 范围 | 命令 / 方式 | 结果 | 备注 |
|---|---|---|---|---|
| 2026-04-29 | docs 目录整理 | 文档路径与旧引用扫描 | 通过 | 已确认 docs 内旧路径引用已同步到新结构。 |
| 2026-04-29 | Task 1 数据库 migration 与类型同步 | `npm run test:knowledge-items` | 通过 | 已覆盖 AI 摘要字段、`aiUsageLogs` schema 和 update payload。 |
| 2026-04-29 | Task 1 数据库 migration 手动执行 | 用户确认 | 完成 | 用户反馈已执行 `db/migrations/0003_ai_assistant.sql`。 |
| 2026-04-29 | Task 2 AI 类型、配置、错误和 Schema | `npm run test:ai` | 通过 | 已覆盖 config、errors、schemas。 |
| 2026-04-29 | Task 2 构建兼容性 | `npm run build` | 通过 | 已确认新增 AI 基础文件不破坏 Next build。 |
| 2026-04-29 | Task 3 Prompt 与 DeepSeek Provider | `npm run test:ai` | 通过 | 已覆盖 prompts，并保留 config、errors、schemas 测试。 |
| 2026-04-29 | Task 3 构建兼容性 | `npm run build` | 通过 | 已确认 OpenAI SDK、Provider 和 DeepSeek 封装不破坏 Next build。 |
| 2026-04-29 | Task 4 AI 使用日志与限流 | `npm run test:ai` | 通过 | 已覆盖 usage limits 和 ai usage logs repository helper。 |
| 2026-04-29 | Task 4 构建兼容性 | `npm run build` | 通过 | 已确认新增日志 repository 和限流逻辑不破坏 Next build。 |
| 2026-04-29 | Task 5 统一 `/api/ai` 路由 | `npm run test:ai` | 通过 | 已覆盖 AI 请求解析和上下文组装。 |
| 2026-04-29 | Task 5 构建兼容性 | `npm run build` | 通过 | 已确认 `/api/ai` 路由可通过 Next build。 |
| 2026-04-29 | Task 6 AI assistant panel model | `node --experimental-strip-types --disable-warning=MODULE_TYPELESS_PACKAGE_JSON src\components\ai\ai-assistant-panel-model.test.mjs` | 通过 | 已覆盖摘要生成、预览、取消、失败后清理旧预览等模型逻辑。 |
| 2026-04-29 | Task 6 knowledge item summary | `node --experimental-strip-types --disable-warning=MODULE_TYPELESS_PACKAGE_JSON src\lib\knowledge\knowledge-item-summary.test.mjs` | 通过 | 已覆盖 summary payload、revalidation 和 action executor 路径。 |
| 2026-04-29 | Task 6 摘要列表展示 | `npm.cmd run test:knowledge-list-item` | 通过 | 已覆盖列表 view model 优先显示非空 AI 摘要并回退正文截断。 |
| 2026-04-29 | Task 6 构建兼容性 | `npm.cmd run build` | 通过 | 本地使用非真实 Supabase public 占位值运行通过。 |
| 2026-04-29 | Task 6 diff 空白检查 | `git diff --check 9511333..HEAD` | 通过 | 已确认实现提交链无 whitespace error。 |
| 2026-04-30 | Task 7A AI model selection 全量 AI 测试 | `npm run test:ai` | 通过 | 已覆盖 registry、provider factory、settings model、assistant model 和 `/api/ai` modelId allowlist 等 AI 测试链路。 |
| 2026-04-30 | Task 7A settings model 单项测试 | `node --experimental-strip-types --disable-warning=MODULE_TYPELESS_PACKAGE_JSON src/app/app/settings/settings-model.test.mjs` | 通过 | 已覆盖设置页模型选择 helper。 |
| 2026-04-30 | Task 7A 摘要列表展示回归 | `npm run test:knowledge-list-item` | 通过 | 已确认 Task 7A 未破坏列表展示 view model。 |
| 2026-04-30 | Task 7A 构建兼容性 | `npm run build` | 通过 | 直接通过；未使用 Supabase public env 占位值。 |
| 2026-04-30 | Task 7A diff 空白检查 | `git diff --check` | 通过 | 已确认当前工作区 diff 无 whitespace error。 |

## 未决问题 / 风险

- DeepSeek 和 Xiaomi MiMo Token Plan 的真实模型名、endpoint 与可用性需要以各 Provider 当前官方 API 为准。
- Vercel AI 环境变量尚未配置；真实 endpoint/key 需要部署者在本地/线上手动配置并验收。
- 未做真实 Xiaomi MiMo Token Plan 端到端调用；真实 AI Provider 调用、失败日志、限流和权限边界仍需在配置 API Key 后手动验收。
- settings panel 交互无组件级测试，但核心选择 helper 已覆盖。
- AI UI 已完成 Task 6 摘要详情页展示与应用、Task 7A 模型选择与 Provider Registry；编辑页/新建页接入、标签/分类、标题/正文整理和移动端端到端仍待后续验证。
- V0.3 涉及外部模型服务，失败重试、超时、返回非 JSON 和限流场景必须作为阻断级验证项。
